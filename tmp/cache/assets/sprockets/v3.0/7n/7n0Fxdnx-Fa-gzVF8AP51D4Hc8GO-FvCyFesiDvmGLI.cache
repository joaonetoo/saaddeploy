I"(function() {
  jQuery(function() {
    return $('input[type=text]').unbind('change').change(function() {
      console.log($(this).closest('form').attr('action'));
      return $.ajax({
        beforeSend: function(xhr) {
          return xhr.setRequestHeader("Accept", "application/json");
        },
        data: "_method=post&" + ($(this).attr('name')) + "=" + (encodeURIComponent($(this).val())) + "&csrf_param=" + (encodeURIComponent($('meta[name=csrf-token]').attr('content'))),
        dataType: "text",
        type: "post",
        url: "" + ($(this).closest('form').attr('action')),
        error: function(data) {
          return alert("Erro!");
        },
        success: function(data) {
          return alert("Success!");
        }
      });
    });
  });

}).call(this);
:ET