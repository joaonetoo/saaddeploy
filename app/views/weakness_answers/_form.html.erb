<%= form_for(@weakness_answer) do |f| %>
  <% if @weakness_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weakness_answer.errors.count, "error") %> prohibited this weakness_answer from being saved:</h2>

      <ul>
      <% @weakness_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :weakness_id %><br>
    <%= f.text_field :weakness_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
