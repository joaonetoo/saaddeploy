<%= form_for(@answer_note) do |f| %>
  <% if @answer_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_note.errors.count, "error") %> prohibited this answer_note from being saved:</h2>

      <ul>
      <% @answer_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.file_field :arquivo %>
  </div>

  <div class="field">
    <%= f.label :observacao %><br>
    <%= f.text_area :observacao %>
  </div>
  <div class="field">
    <%= f.hidden_field :answer_id,  :value => @answer%>
  </div>
  <div class="actions">
    <%= f.submit 'confirmar' %>
  </div>
<% end %>
