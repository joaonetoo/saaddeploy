<%= form_for(@threats_answer) do |f| %>
  <% if @threats_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@threats_answer.errors.count, "error") %> prohibited this threats_answer from being saved:</h2>

      <ul>
      <% @threats_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :threat_id %><br>
    <%= f.text_field :threat_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
