<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.1.135/jspdf.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/2.0.16/jspdf.plugin.autotable.js"></script>
<%= javascript_include_tag "createPdf"%>
<%= javascript_include_tag "createCsv"%>
<div class="panel panel-default">
  <div class="panel-heading">
    <center><h4></i>Comparar Âncoras de Carreira</h4></center>
  </div>
    <div class="row mt">
      <div class="col-lg-12">
        <div class="panel-body">         
          <center><p>Agora que você selecionou o(s) aluno(s), selecione duas diferentes datas para visualizar</p></center>
              <%= form_tag(compare_by_date_results_path, method: 'post', :remote => true) do %>
                <% if @allUsers == false  && @allUsers2 == false  %>
                  <div class="row mt">
                      <div class="col-lg-6">
                        <%= hidden_field_tag "selecao", @selecao %>
                      <div class="col-lg-4"  style="right: -200px">
                        <center><h4><%= @selecao %></h4></center>

                        <%= select_tag(:data_final, options_for_select(@datas),:class => "form-control") %>
                      </div>
                        <% @results.map(&:id).each do |id| %>
                          <%= hidden_field_tag "results[]", id %>
                        <% end %>
                         <%= hidden_field_tag "allUsers", @allUsers %>

                          <% @users.map(&:id).each do |uu| %>
                            <%= hidden_field_tag "users[]", uu %>
                          <% end %>

                </div>
                <div class="col-lg-6">
                <%= hidden_field_tag "selecao2", @selecao2 %>
                   <div class="col-lg-4"  style="right: -50px">
                        <center><h4><%= @selecao2 %></h4></center>

                        <%= select_tag(:data_final2, options_for_select(@datas2),:class => "form-control") %>
                  </div>
                    <% @results2.map(&:id).each do |id| %>
                      <%= hidden_field_tag "results2[]", id %>
                    <% end %>
                    <% @users2.map(&:id).each do |uu| %>
                        <%= hidden_field_tag "users2[]", uu %>
                    <% end %>
                    <%= hidden_field_tag "allUsers2", @allUsers2 %>

                </div>
            </div>
                <% end %>
          <% if @allUsers == true  && @allUsers2 == true  %>
                <div class="row mt">

                <div class="col-lg-6">
                   <div class="col-lg-4"  style="right: -200px">
                        <center><h4><%= @selecao %></h4></center>
                    </div>
                         <%= hidden_field_tag "allUsers", @allUsers %>
                          <% @users.map(&:id).each do |uu| %>
                            <%= hidden_field_tag "users[]", uu %>
                          <% end %>
                </div>

                <div class="col-lg-6">
                   <div class="col-lg-4"  style="right: -50px">
                        <center><h4><%= @selecao2 %></h4></center>
                    </div>
                <%= hidden_field_tag "selecao2", @selecao2 %>
             <%= hidden_field_tag "allUsers2", @allUsers2 %>
              <% @users2.map(&:id).each do |uu| %>
                <%= hidden_field_tag "users2[]", uu %>
              <% end %>
                </div>
                </div>
          <% end %>
        <% if @allUsers == false  && @allUsers2 == true  %>
                  <div class="row mt">
                      <div class="col-lg-6">
                        <%= hidden_field_tag "selecao", @selecao %>
                      <div class="col-lg-4"  style="right: -200px">
                        <center><h4><%= @selecao %></h4></center>

                        <%= select_tag(:data_final, options_for_select(@datas),:class => "form-control") %>
                      </div>
                        <% @results.map(&:id).each do |id| %>
                          <%= hidden_field_tag "results[]", id %>
                        <% end %>
                         <%= hidden_field_tag "allUsers", @allUsers %>

                          <% @users.map(&:id).each do |uu| %>
                            <%= hidden_field_tag "users[]", uu %>
                          <% end %>
                      </div>
                      <div class="col-lg-6">
                         <div class="col-lg-4"  style="right: -50px">
                              <center><h4><%= @selecao2 %></h4></center>
                          </div>
                      <%= hidden_field_tag "selecao2", @selecao2 %>
                      <%= hidden_field_tag "allUsers2", @allUsers2 %>
                      <% @users2.map(&:id).each do |uu| %>
                          <%= hidden_field_tag "users2[]", uu %>
                      <% end %>
                    </div>
                </div>



        <% end %>
        <% if @allUsers == true  && @allUsers2 == false  %>
              <div class="row mt">
                <div class="col-lg-6">
                   <div class="col-lg-4"  style="right: -200px">
                        <center><h4><%= @selecao %></h4></center>
                    </div>
                         <%= hidden_field_tag "allUsers", @allUsers %>
                          <% @users.map(&:id).each do |uu| %>
                            <%= hidden_field_tag "users[]", uu %>
                          <% end %>
                </div>
                <div class="col-lg-6">
                <%= hidden_field_tag "selecao2", @selecao2 %>
                   <div class="col-lg-4"  style="right: -50px">
                        <center><h4><%= @selecao2 %></h4></center>

                        <%= select_tag(:data_final2, options_for_select(@datas2),:class => "form-control") %>
                  </div>
                    <% @results2.map(&:id).each do |id| %>
                      <%= hidden_field_tag "results2[]", id %>
                    <% end %>
                    <% @users2.map(&:id).each do |uu| %>
                        <%= hidden_field_tag "users2[]", uu %>
                    <% end %>
                    <%= hidden_field_tag "allUsers2", @allUsers2 %>

                </div>
            </div>

        <% end %>

            </div>
            <div class="row mt">
                <div class="col-lg-12">
 <center><%= submit_tag "Comparar", class: "btn btn-default"%></center>
                  <% end %>
                </div>
            </div>
            <div class="row mt">
                  <div class="col-lg-6">
                      <section id="unseen">
                        <table id="tabela_resultados" class="table table-bordered table-striped table-condensed">
                          <thead>
                            <tr>
                              <th>Ordem</th>
                              <th>Pontuação</th>
                              <th>Âncoras</th>
                            </tr>
                          </thead>
                          <tbody  id="media"></tbody>
                        </table>
                      </section>
                  </div>
                  <div class="col-lg-6">
                      <section id="unseen">
                      <table id="tabela_resultados2" class="table table-bordered table-striped table-condensed">
                        <thead>
                          <tr>
                            <th>Ordem</th>
                            <th>Pontuação</th>
                            <th>Âncoras Predominantes</th>
                          </tr>
                      </thead>
                      <tbody  id="media2"></tbody>
                    </table>
                  </section>
                </div>
            </div>
            <div class="row mt">
                <div class="col-lg-12">
                      <div id="graphic"></div>
                </div>
            </div>
      </div>
    </div>
</div>