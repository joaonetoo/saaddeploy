<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :data_abertura %><br>
    <%= f.date_select :data_abertura %>
  </div>
  <div class="field">
    <%= f.label :turno %><br>
    <%= f.text_field :turno %>
  </div>
  <div class="field">
    <%= f.label :institution_id %><br>
    <%= f.text_field :institution_id, data: {autocomplete_source: search_institutions_url}, class: "form-control", :placeholder => "Comece a digitar para auto completar o nome" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
