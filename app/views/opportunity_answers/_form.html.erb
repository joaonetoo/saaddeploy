<%= form_for(@opportunity_answer) do |f| %>
  <% if @opportunity_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opportunity_answer.errors.count, "error") %> prohibited this opportunity_answer from being saved:</h2>

      <ul>
      <% @opportunity_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :opportunity_id %><br>
    <%= f.text_field :opportunity_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
