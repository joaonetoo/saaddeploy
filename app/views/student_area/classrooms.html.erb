<h2><i class="fa fa-angle-right"></i>Minhas turmas</h2>
<div class="row mt">
    <div class="col-lg-12">
      <div class="content-panel">
            <%= form_tag({ :controller => "students_area", :action => "list_classroom"}, { :id => "form2"}) do |f| %>
            <div class="field">
                <%= label_tag :Turma %>
                <%= grouped_collection_select(nil, :classroom_id, @subjects, :classrooms, :nome, :id, :codigo)%>
            </div>
            <%= submit_tag "Enviar", :class => "btn btn-info btn-sm" %>
            <% end %>
      </div>
    </div>
</div>