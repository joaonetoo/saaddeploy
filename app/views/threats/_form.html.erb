<% modal ||= false %>
<% remote = modal ? true : false %>
<%= form_for(Threat.new, remote: remote, html: {role: :form, 'data-model' => 'Threat'}) do |f| %>

  <div class="field">
    <%= f.label :ameaca, class: 'control-label' %><br>
    <%= f.text_area :text, class: 'form-control',rows:4  %>
    <span class="help-block"></span>
  </div>
  <div class="field">
    <%= f.hidden_field :plano_id, :value => @plano.id %>
  </div>
  <div class="actions">
    <center><%= f.submit "Criar ameaça", class: "btn btn-default" %></center>
  </div>
<% end %>
