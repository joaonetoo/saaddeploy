<%= form_for(@threat) do |f| %>
  <% if @threat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@threat.errors.count, "error") %> prohibited this threat from being saved:</h2>

      <ul>
      <% @threat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :plano_id %><br>
    <%= f.text_field :plano_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
