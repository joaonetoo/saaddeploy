<p id="notice"><%= notice %></p>
  <div class="panel panel-default">
    <div class="panel-heading">
        <h4>Professores</h4>
        <%# para colocar o botao verde par aa direita %>
    </div>
    <!-- /.panel-heading -->
    <div class="panel-body">
             <div class="table-responsive">
                      <section id="unseen">
                          <div id = "dados">
                          <table class="table table-bordered table-striped table-condensed">
                            <thead>
                              <tr>
                                <th>Foto</th>
                                <th>Nome</th>
                                <th>Matrícula</th>
                                <th>Telefone</th>
                                <!--<th>Biografia</th>
                                <th>Matricula</th>-->
                                <th>Email</th>
                                <!--<th>Autorizado</th>-->
                                <th colspan="2"><center>Ações</center></th>
                              </tr>
                            </thead>
                            <tbody>
                              <% @teachers.each do |teacher| %>
                                <tr>
                                  <td><center><%= image_tag teacher.avatar.url(:thumb), class: "photos_users", width:40, height:40  %></center></td>
                                  <td><%= teacher.nome %></td>
                                  <td><%= teacher.matricula %></td>
                                  <td><%= teacher.telefone %></td>
                                  <!--<td><%= teacher.biografia %></td>
                                  <td><%= teacher.matricula %></td>-->
                                  <td><%= teacher.email %></td>
                                  <!--<td><%= teacher.approved %></td>-->

                                  <td>
                                  <%= link_to 'Excluir', teacher, method: :delete, data: { confirm: 'Deseja realmente excluir?' } %></td>
                                </tr>
                              <% end %>
                            </tbody>
                          </table>
                      </div>
                    </section>
                </div>
            </div>
      </div>
