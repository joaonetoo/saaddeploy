<%= javascript_include_tag 'search_learning_admin' %>
<%= form_tag({ :controller => "learning_results", :action => "analytic_list"}, { :id => "form2"}) do |f| %>
<h2>Seleção 1</h2>
<div class="field">
    <%= hidden_field_tag :institution_id, @institution.id %>

    <%= label_tag :Campus %>
    <%= collection_select(nil, :campu_id, @campus, :id, :name)%>

    <%= label_tag :Centro %>
    <%= collection_select(nil, :center_id, @centers, :id, :name)%>

    <%= label_tag :Curso %>
    <%= collection_select(nil, :course_id, @courses, :id, :nome)%>

    <%= label_tag :Disciplina %>
    <%= collection_select(nil, :subject_id, @subjects, :id, :nome)%>

    <%= label_tag :Turma %>
    <%= collection_select(nil, :classroom_id, @classrooms, :id, :codigo)%>

    <%= label_tag :Usuarios %>
    <%= collection_select(nil, :users_id, @students, :id, :nome)%>
  </div>

  <h2>Seleção 2</h2>
  <div class="field">
    <%= hidden_field_tag :institution2_id, @institution.id %>

    <%= label_tag :Campus %>
    <%= collection_select(nil, :campu2_id, @campus, :id, :name)%>

    <%= label_tag :Centro %>
    <%= collection_select(nil, :center2_id, @centers, :id, :name)%>

    <%= label_tag :Curso %>
    <%= collection_select(nil, :course2_id, @courses, :id, :nome)%>

    <%= label_tag :Disciplina %>
    <%= collection_select(nil, :subject2_id, @subjects, :id, :nome)%>

    <%= label_tag :Turma %>
    <%= collection_select(nil, :classroom2_id, @classrooms, :id, :codigo)%>

    <%= label_tag :Usuarios %>
    <%= collection_select(nil, :users2_id, @students, :id, :nome)%>
  </div>

<%= submit_tag "Enviar" %>
<% end %>