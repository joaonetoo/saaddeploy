$('#media').html('<%= escape_javascript render (@learning_results) %>');




$('#media2').html('<%= escape_javascript render (@learning_results) %>');





$('#container').highcharts({

        chart: {
            polar: true,
            type: 'line'
        },

        title: {
            text: 'Comparação das Ancoras',
            x: -80
        },

        pane: {
            size: '80%'
        },

        xAxis: {
            categories: ['Experiência concreta', 'Divergente', 'Observação reflexiva', 'Assimilador', 'Conceituação Abstrata', 'Convergente', 'Experimentação ativa', 'Acomodador'],
            tickmarkPlacement: 'on',
            lineWidth: 0
        },

        yAxis: {
            gridLineInterpolation: 'polygon',
            lineWidth: 0,
            min: 0
        },

        tooltip: {
            shared: true,
            pointFormat: '<span style="color:{series.color}">Pontuação: <b>{point.y:,.2f}</b><br/>'
        },

        legend: {
            align: 'right',
            verticalAlign: 'top',
            y: 70,
            layout: 'vertical'
        },

        series: [{
            name: '<%= @selecao %>',
            data: [<%= @mediaEc %>, <%= @mediaDi %>, <%= @mediaOr %>, <%= @mediaAs %>, <%= @mediaCa%>, <%= @mediaCo %>, <%= @mediaEa %>, <%= @mediaAc %>],
            pointPlacement: 'on'
        }, {
            name: '<%= @selecao2 %>',
            data: [<%= @mediaEc2 %>, <%= @mediaDi2 %>, <%= @mediaOr2 %>, <%= @mediaAs2 %>, <%= @mediaCa2%>, <%= @mediaCo2 %>, <%= @mediaEa2 %>, <%= @mediaAc2 %>],
            pointPlacement: 'on'
        }]

});