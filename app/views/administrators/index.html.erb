<p id="notice"><%= notice %></p>
  <div class="panel panel-default">
    <div class="panel-heading">
        <h4>Administradores</h4>
        <%# para colocar o botao verde par aa direita %>
        <%= link_to new_administrator_path, class: "btn btn-primary btn-circle pull-right", style:  "margin-top : -35px" do  %> 
            <i class="fa fa-plus"></i>
        <% end %>
    </div>
    <!-- /.panel-heading -->
    <div class="panel-body">
             <div class="table-responsive">
                      <section id="unseen">
                          <div id = "dados">
                          <table class="table table-bordered table-striped table-condensed">
                            <thead>
                              <tr>
                                <th>Foto</th>
                                <th>Nome</th>
                                <th>Telefone</th>
                                <th>Endereço</th>
                                <!--<th>Biografia</th>
                                <th>Matricula</th>-->
                                <th>Email</th>
                                <!--<th>Autorizado</th>-->
                                <th colspan="2"><center>Ações</center></th>
                              </tr>
                            </thead>
                            <tbody>
                              <% @administrators.each do |administrator| %>
                                <tr>
                                  <td><center><%= image_tag administrator.avatar.url(:thumb), class: "photos_users", width:40, height:40  %></center></td>
                                  <td><%= administrator.nome %></td>
                                  <td><%= administrator.telefone %></td>
                                  <td><%= administrator.endereco %></td>
                                  <!--<td><%= administrator.biografia %></td>
                                  <td><%= administrator.matricula %></td>-->
                                  <td><%= administrator.email %></td>
                                  <!--<td><%= administrator.approved %></td>-->

                                  <td>
                                  <%= link_to 'Excluir', administrator, method: :delete, data: { confirm: 'Deseja realmente excluir?' } %></td>
                                </tr>
                              <% end %>
                            </tbody>
                          </table>
                      </div>
                    </section>
                </div>
            </div>
      </div>
