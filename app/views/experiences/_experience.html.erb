 <% @experiences.each do |experience| %>
  <div class="resume-item d-flex flex-column flex-md-row mb-5">
    <div class="resume-content mr-auto" >
      <p><b> Instituição: </b><%= experience.institution %> · <b>Cargo/Função:</b> <%= experience.office %> · 
   	<b>Período: </b><%= experience.date_start.strftime("%d/%m/%Y")  %> - 	<%= experience.date_end.strftime("%d/%m/%Y")  %>
<br>
	<b>Resumo:</b><%=experience.description %>
      <%= link_to 'Deletar', experience, method: :delete,class: "btn btn-default btn-sm btn-login", remote: true, data: { confirm: 'Are you sure?' }%>        <%= link_to 'Editar', edit_experience_path(experience.id), class: "btn btn-default btn-sm btn-login",remote:true   %> 
</p>
    </div>
  </div>
 <% end %>

