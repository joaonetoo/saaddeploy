<%# Conditionally set remote: true. Also, passing model name as data attribute %>
<% modal ||= false %>
<% remote = modal ? true : false %>
<%= form_for(Purpose.new,remote: true, method: "puts", html: {role: :form, 'data-model' => 'strength'}) do |f| %>
  <div class="field">
    <%= f.label :descricao, class: 'control-label' %><br>
    <%= f.text_area :description, class: 'form-control',rows:4  %>
    <span class="help-block"></span>
  </div>
  <div class="field">
  <%= f.hidden_field :curriculum_id, :value => locals[:curriculum] %>
  </div><br>
  <div class="actions">
    <center><%= f.submit 'Criar força', class: "btn btn-default" %></center>
  </div>
<% end %>
